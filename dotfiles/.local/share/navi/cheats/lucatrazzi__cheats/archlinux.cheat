% archlinux, usb, boot

# how to burn a iso to usb
google-chrome-stable https://linoxide.com/create-bootable-arch-linux-usb-drive/

% archlinux, window

# how to get window name by pointing with mouse
xprop WM_CLASS | grep -o '"[^"]*"' | head -n 1

% archlinux, disk, size, folder

# calculate folder sizes
gdu
